<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="EyeUnderstand.mybatis.EyeunderstandMapper">

	<!-- <select id="selectPoint" parameterType="FavoriteVO">
	    select POINT favorite where FAVORITEWORD = #{FAVORITEWORD}
	</select>    --> 
    
	<select id="memberLogin" parameterType="MemberVO"
		resultType="MemberVO">
		select * from eyemember where ID=#{id, jdbcType=VARCHAR}
		and PW=#{pw, jdbcType=VARCHAR}
	</select>

	<select id="getTestList" parameterType="java.util.Map"
		resultType="WordVO">
		select * from word where words like ('${serchInput}%')
	</select>
	
	<insert id="favoriteInsert" parameterType="FavoriteVO">
        insert into favorite values 
        (#{ID, jdbcType=VARCHAR}, #{FAVORITEWORD, jdbcType=VARCHAR}, #{POINT, jdbcType=INTEGER})
    </insert>

</mapper>